# This file belongs in Nginx's http context, for example /etc/nginx/conf.d/.
limit_req_zone $binary_remote_addr zone=viking_inquiry_per_ip:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=viking_admin_login_per_ip:10m rate=5r/m;
